{% extends 'base.html.twig' %}

{% block title %}{{ pokemon.title }}{% endblock %}

{% block content %}

    <main>
        <section id="descriptionPokemon">
            <article class="descriptionPokemonbdd">
                <div><img src="{{ pokemon.image }}" alt="{{ pokemon.title }}"></div>
                <h2>{{ pokemon.title }}</h2>
                <p>{{ pokemon.description }}</p>
            </article>
        </section>
    </main>

{% endblock %}